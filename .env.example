# Discord app credentials
DISCORD_APP_ID=
DISCORD_BOT_TOKEN=
DISCORD_PUBLIC_KEY=

# Optional: register commands to a single guild for faster updates
DISCORD_COMMAND_GUILD_ID=

# Access settings
ACCESS_FRONTEND_BASE_URL=https://access.governance.so
RPC_ENDPOINT=https://api.mainnet-beta.solana.com
CLUSTER=mainnet-beta
CHECK_MODE=simulate
ONCHAIN_CHECKER_KEYPAIR_PATH=

# Revalidation + sync behavior
DEFAULT_RECHECK_INTERVAL_SEC=86400
DRY_RUN_SYNC=false
BASIC_IDENTITY_CHECK_MODE=true
MAX_SYNC_JOBS_PER_CRON=3
MAX_USERS_PER_SYNC=500
RPC_REQUEST_TIMEOUT_MS=8000

# Optional security for callback and cron
VERIFY_SHARED_SECRET=
CRON_SECRET=

# Vercel KV (required for persistent state in serverless)
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
KV_URL=
REDIS_URL=
KV_KEY_PREFIX=grape-access-discord-bot:v1

# Optional bootstrap mappings (JSON array)
# Example:
# [{"guildId":"123","gateId":"abc","daoId":"my-dao","passRoleId":"456","failAction":"remove_role","enabled":true}]
BOOTSTRAP_GATES_JSON=

LOG_LEVEL=info
